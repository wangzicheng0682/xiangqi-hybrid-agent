# 象棋AI混合代理系统 - 项目DNA

> 本文档是项目的"基因图谱"，AI开发者只需阅读本文档即可理解项目全貌。

**版本**: v1.0
**最后更新**: 2026-03-14
**阅读时间**: ~5分钟

---

## ⚖️ 项目宪法（最高优先级）

> 以下两条原则为项目最高准则，所有AI Code必须遵守，不得违反。

### 第一条：模块化开发原则

**核心要求**：项目必须支持在不同AI Code对话中独立开发不同模块。

**模块划分**：

| 模块 | 目录 | 职责 | 可独立开发 |
|------|------|------|------------|
| 标签检测器 | `core/rules/` | 45个标签检测逻辑 | ✅ |
| Agent工具 | `core/llm/agent_tools.py` | 10个LLM可调用工具 | ✅ |
| **思维链模板** | `core/llm/thinking_templates.py` | 阶段感知思维框架 | ✅ |
| 引擎封装 | `core/engine/` | Pikafish引擎集成 | ✅ |
| 知识图谱 | `core/kg/` | Neo4j棋谱检索 | ✅ |
| 视觉识别 | `core/vision/` | YOLO棋盘识别 | ✅ |
| API层 | `api/` | FastAPI端点 | ✅ |
| 前端 | `frontend/` | React UI | ✅ |

**模块接口规范**：
- 每个模块必须定义清晰的输入/输出接口
- 模块间通过接口通信，不得直接访问内部实现
- 新增模块必须先在DNA.md中注册

---

### 第二条：文档维护标准

**核心要求**：所有AI Code在修改代码前必须先阅读并遵守本文档标准，否则无权修改。

**文档层级**：

```
┌─────────────────────────────────────────────┐
│  DNA.md（本文件）                            │  ← 宪法级别，修改需用户批准
│  - 项目愿景、架构、核心能力                  │
│  - 模块划分、接口规范                        │
│  - 技术决策、开发路线                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  STATE.md                                   │  ← 状态级别，AI可更新进度
│  - 当前进度、待办事项                        │
│  - 最近更新记录                              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  reference/                                 │  ← 技术规范，需同步更新
│  - tag_reference.md（标签定义）             │
│  - agent_tools_spec.md（工具规范）          │
└─────────────────────────────────────────────┘
```

**AI Code修改权限**：

| 文件类型 | AI Code权限 | 修改前要求 |
|----------|-------------|------------|
| DNA.md | ⚠️ 需用户批准 | 必须先阅读全文 |
| STATE.md | ✅ 可直接更新 | 更新后通知用户 |
| reference/*.md | ✅ 可同步更新 | 代码变更时同步 |
| 核心代码 | ✅ 可修改 | 必须先读DNA.md对应章节 |
| 接口定义 | ⚠️ 需用户批准 | 必须先在DNA.md中更新 |

**修改流程**：
```
1. 阅读DNA.md对应章节
2. 确认修改不违反模块边界
3. 如涉及接口变更 → 先更新DNA.md → 用户批准 → 再改代码
4. 如仅内部实现 → 直接修改 → 同步更新STATE.md
```

---

## 一、项目愿景

### 一句话定义
让AI像教练一样**读懂棋局**、**讲解棋局**，而不是念坐标。

### 核心价值
| 价值 | 实现方式 | 状态 |
|------|----------|------|
| **零幻觉** | 规则标签100%准确，Evidence Map约束LLM | ✅ |
| **教练式讲解** | 基于事实的因果分析，不是念坐标 | ✅ |
| **双模式响应** | 快速<1秒 / 深度3-5秒（SSE流式） | ✅ |
| **棋书知识** | RAG检索棋书内容，融入讲解 | ⏳ 待录入 |
| **棋谱检索** | Neo4j存储19万局历史对局 | 🔄 框架已搭建 |
| **现实棋盘识别** | YOLO拍照识别现实棋盘 | ⏳ 规划中 |

### 目标用户
- 象棋学习者（教学讲解）
- 棋谱研究者（历史对局分析）
- 比赛选手（复盘分析）

---

## 二、系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (React)                          │
│  棋盘展示 / 走法输入 / 讲解展示 / 模式切换                    │
└─────────────────────────────────────────────────────────────┘
                              ↓ API调用
┌─────────────────────────────────────────────────────────────┐
│                     后端 (FastAPI)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  快速模式   │  │  深度模式   │  │  棋谱模式   │          │
│  │  < 1秒      │  │  3-5秒 SSE  │  │  走法标注   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      核心能力层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │标签检测器│ │Agent工具 │ │引擎分析  │ │局面解析  │        │
│  │(45标签)  │ │(10工具)  │ │(Pikafish)│ │(规则引擎)│        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据/知识层                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │Neo4j棋谱 │ │RAG棋书   │ │YOLO识别  │ │Pikafish  │        │
│  │(19万局)  │ │(待录入)  │ │(规划中)  │ │(引擎)    │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 核心数据流

```
FEN/图片输入 → 局面解析 → 标签检测 → Evidence Map → LLM讲解 → 前端展示
```

### 关键技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 特征提取 | 规则脚本（非模型） | 100%准确，无幻觉，可解释 |
| 向量检索 | 暂不训练Transformer | T0期间规则足够，降低复杂度 |
| LLM约束 | Evidence Map | 事实锚定，防止LLM编造 |
| 引擎 | Pikafish | 开源中国象棋引擎 |
| 图数据库 | Neo4j | 棋谱关系查询，相似局面检索 |

---

## 三、三大核心能力

### 3.1 标签检测体系（45个标签）

**设计原则**：
- 100%规则硬判定，置信度二元化（0或1）
- 每个标签必须绑定具体棋子坐标
- 格式：`颜色兵种-(x,y)`，如 `红车-(5,8)`

**九大层级**：

| 层级 | 标签数 | 核心标签 | 状态 |
|------|--------|----------|------|
| 1. 将杀困毙层 | 3 | is_check, is_checkmate, is_stalemate | ✅ |
| 2. 捉子层 | 3 | is_attack_unprotected, is_mutual_attack | ✅ |
| 3. 闪击抽将层 | 3 | is_discovered_check, is_double_attack_with_check | ✅ |
| 4. 牵制串打层 | 2 | is_pinned, is_cannon_double_attack | ✅ |
| 5. 标准杀法层 | 8 | 马后炮、铁门栓、双车错、卧槽马等 | ✅ |
| 6. 子力状态层 | 2 | piece_is_unprotected, cannon_has_platform | ✅ |
| 7. 走法质量层 | 6 | move_is_blunder, move_is_capture, move_gives_check等 | ✅ |
| 8. 战略评估层 | 5 | controls_open_file, has_active_pieces, piece_coordination等 | ✅ |
| 9. 引擎对齐层 | 3 | engine_only_move, engine_best_by_margin | ✅ |

**核心文件**：
- `core/rules/tactical_tags.py` - 标签定义
- `core/rules/tactical_detector.py` - 检测器实现

---

### 3.2 Agent工具调用（10个工具）

**设计原则**：
- 原子化：每个工具只做一件事
- 可组合：工具之间可以串联
- 返回结构化：方便LLM理解和展示

**四类工具**：

| 类别 | 工具 | 用途 |
|------|------|------|
| 棋子关系 | get_piece_attacks | 查询某棋子攻击范围 |
|          | get_piece_defenders | 查询谁在保护某棋子 |
|          | get_threats_to_piece | 查询谁在威胁某棋子 |
|          | get_piece_relations | 综合查询（一站式） |
| 走法分析 | analyze_move | 深度分析某步棋的效果 |
|          | compare_moves | 对比多步棋的优劣 |
|          | get_forcing_sequence | 分析强制序列 |
| 引擎分析 | engine_deep_analysis | 引擎深度分析 |
|          | engine_alternatives | 候选走法对比 |
| 战略分析 | analyze_position_strategy | 局面战略分析 |

**调用约束**：
- 最多3轮调用
- 每轮最多3个工具并行
- 总耗时<5秒
- 每个工具调用有思考过程反馈（SSE）

**核心文件**：`core/llm/agent_tools.py`

---

### 3.3 双模式讲解

**快速模式**（<1秒）：
- 预计算Evidence Map
- 直接返回标签+引擎数据
- 适合：对弈提示、引擎推荐解释

**深度思考模式**（3-5秒）：
- Agent主动调用工具分析
- SSE流式返回思考过程
- 适合：棋谱讲解、复杂局面分析

**三种讲解场景**：

| 场景 | 问题示例 | 数据来源 |
|------|----------|----------|
| 提示模式 | "我刚才走得怎么样？" | 走法质量标签 |
| 引擎解释 | "为什么推荐这步？" | 引擎对齐标签 |
| 棋谱模式 | "这步棋意图是什么？" | Agent深度分析 |

---

## 四、数据/知识层

### 4.1 Neo4j棋谱数据库

**用途**：存储19万局历史对局，支持相似局面检索

**数据模型**：
```
(:Game {id, red, black, result, date})
(:Position {fen, eval, phase})
(:Move {uci, chinese, quality})
(:Position)-[:HAS_MOVE]->(:Move)-[:LEADS_TO]->(:Position)
(:Game)-[:HAS_POSITION]->(:Position)
```

**状态**：🔄 框架已搭建，数据已导入部分

**相关文件**：`core/kg/neo4j_kg.py`

---

### 4.2 RAG棋书知识库

**用途**：检索象棋教学书籍，为讲解提供棋理知识

**规划内容**：
- 象棋入门教程
- 战术组合手册
- 开局原理
- 残局定式

**状态**：⏳ 未录入

**待完成**：
- [ ] 收集棋书PDF
- [ ] 文本提取和清洗
- [ ] 向量化入库
- [ ] 检索接口实现

**相关文件**：`core/llm/rag_retriever.py`（待实现）

---

### 4.3 YOLO棋盘识别

**用途**：拍照识别现实中的棋盘，转换为FEN

**功能流程**：
```
照片输入 → YOLO检测棋子位置 → 棋子分类 → 生成FEN → 进入分析流程
```

**状态**：⏳ 规划中

**待完成**：
- [ ] 收集棋盘图片数据集
- [ ] 标注棋子位置和类别
- [ ] 训练YOLO模型
- [ ] 集成到API

**相关文件**：
- `core/vision/board_detector.py`（待实现）
- `core/vision/piece_classifier.py`（待实现）

---

## 五、API设计

### 核心端点

| 端点 | 方法 | 用途 | 模式 |
|------|------|------|------|
| `/api/analyze/quick` | POST | 快速分析 | 快速模式 |
| `/api/analyze/deep` | POST | 深度分析(SSE) | 深度模式 |
| `/api/move-quality` | POST | 走法质量检测 | - |
| `/api/tactical-tags` | POST | 标签检测 | - |
| `/api/board-structure` | POST | 局面结构解析 | - |
| `/api/legal-moves` | POST | 合法走法查询 | - |

### Evidence Map格式

```json
{
  "facts_rules": [
    {
      "tag": "is_attack_unprotected",
      "confidence": 1.0,
      "bind_pieces": ["红车-(5,8)", "黑马-(7,8)"],
      "description": "红车攻击黑无根马"
    }
  ],
  "engine_eval": {"cp": 150, "best": "h2e2"},
  "phase": "中局",
  "initiative": "红方"
}
```

### SSE流式响应（深度模式）

```
data: {"type": "thinking", "stage": 1, "message": "分析局面特征..."}
data: {"type": "thinking", "stage": 2, "tool": "get_piece_relations", "target": "红车"}
data: {"type": "thinking", "stage": 3, "finding": "红车控制中路，威胁黑马"}
data: {"type": "result", "explanation": "...", "confidence": "high"}
```

---

## 六、LLM调教指南

### 6.1 System Prompt设计

```
你是象棋教练AI，擅长分析局面和讲解棋理。

【你的能力】
- 你可以调用10个分析工具（见工具列表）
- 你会收到Evidence Map（事实锚点），必须基于此回答
- 你擅长用通俗语言解释复杂的战术和战略

【你的约束】
- 所有事实必须来自Evidence Map或工具调用结果
- 不得编造棋子位置、走法或评估
- 不确定时说"需要进一步分析"，不要猜测

【你的思考框架】
1. 先看Evidence Map中的标签，理解局面发生了什么
2. 如果需要深入，调用工具验证假设
3. 用"因为...所以..."的逻辑组织讲解
4. 根据用户水平调整讲解深度
```

### 6.2 工具描述优化

**原则**：告诉LLM"什么时候用"，而不仅仅是"做什么"

**示例**：
```python
def get_piece_attacks(fen, piece):
    """
    获取棋子攻击范围

    【什么时候用】
    - 当你想知道某个棋子能攻击什么时
    - 当你分析"这步棋威胁了什么"时
    - 当你想解释"为什么要走这步"时

    【参数】
    - fen: 当前局面
    - piece: 棋子标识，如 "红车" 或 "r" 或坐标 "(5,8)"
    """
```

### 6.3 思考过程引导

**战术分析思考模板**：
```
1. 【局面扫描】阶段、主动权、威胁/机会
2. 【走法分析】改变了什么、新威胁、解决的问题
3. 【对比思考】更好选择、风险
4. 【总结输出】意图概括、建议
```

### 6.4 错误纠正

- **工具调用约束**：System Prompt中明确调用规则
- **结果验证**：后端验证工具返回是否合理
- **回退机制**：Agent分析失败时回退到快速模式

---

## 七、项目结构

```
xiangqi-hybrid-agent/
├── api/                        # FastAPI后端
│   └── main.py                 # API端点定义
├── frontend/                   # React前端
│   └── src/
│       ├── components/         # UI组件
│       ├── store/              # 状态管理
│       └── services/           # API服务
├── core/                       # 核心模块
│   ├── rules/                  # 规则引擎
│   │   ├── tactical_tags.py    # 45个标签定义
│   │   └── tactical_detector.py # 标签检测器
│   ├── llm/                    # LLM相关
│   │   ├── client.py           # LLM客户端
│   │   └── agent_tools.py      # 10个Agent工具
│   ├── engine/                 # 引擎封装
│   │   └── pikafish_engine.py  # Pikafish封装
│   ├── kg/                     # 知识图谱
│   │   └── neo4j_kg.py         # Neo4j封装
│   ├── semantic/               # 语义层
│   │   └── board_structure_parser.py
│   └── vision/                 # 视觉识别(待实现)
│       ├── board_detector.py   # YOLO棋盘检测
│       └── piece_classifier.py # 棋子分类
├── data/                       # 数据目录
│   ├── init_data/              # 原始PGN(19万局)
│   └── processed/              # 处理后数据
├── docs/                       # 文档
│   ├── DNA.md                  # 项目DNA(本文件)
│   ├── STATE.md                # 当前状态
│   └── reference/              # 参考文档
└── tests/                      # 测试
```

---

## 八、快速启动

### 环境要求

- Python 3.10+
- Node.js 18+
- Neo4j 5.x (端口7687，可选)

### 启动命令

```bash
# 1. 启动Neo4j (如果需要棋谱检索)
neo4j start

# 2. 启动后端
python -m uvicorn api.main:app --host 0.0.0.0 --port 8002

# 3. 启动前端
cd frontend && npm run dev

# 4. 访问
# http://localhost:3000
```

### 测试

```bash
# 运行所有测试
pytest -q

# 测试标签检测
python -c "
from core.rules.tactical_detector import TacticalDetector
d = TacticalDetector()
print(f'Total tags: 45')
"

# 测试Agent工具
python -c "
from core.llm.agent_tools import AgentTools
tools = AgentTools()
print(f'Agent tools: 10')
"
```

---

## 九、开发路线图

### ✅ 已完成
- [x] 标签体系定义(45个)
- [x] 标签检测器实现
- [x] Agent工具实现(10个)
- [x] 双模式API
- [x] 前端基础框架
- [x] Neo4j基础框架
- [x] Pikafish引擎集成
- [x] **特级大师思维链框架设计**

### 🔄 进行中
- [ ] 开局思维链实现（阶段检测+出子效率）
- [ ] 标签检测准确性验证（经典棋局测试）
- [ ] Neo4j数据完善
- [ ] LLM System Prompt优化

### ⏳ 待开始
- [ ] 中局/残局思维链
- [ ] RAG棋书知识库录入
- [ ] YOLO棋盘识别
- [ ] 性能优化（缓存机制）
- [ ] 三档讲解模式（初学者/进阶/教练）

---

## 十、参考文档

详细技术规范请参阅 `docs/reference/` 目录：

| 文档 | 内容 |
|------|------|
| `thinking_framework.md` | **特级大师思维链框架**（阶段感知分析） |
| `tag_reference.md` | 45个标签完整定义和检测算法 |
| `agent_tools_spec.md` | 10个Agent工具接口和调用示例 |
| `api_spec.md` | API完整规范和请求/响应示例 |

---

**文档版本**: v1.0
**维护者**: 项目团队
**更新频率**: 重大变更时更新
